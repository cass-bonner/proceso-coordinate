/*! proceso-coordinate-capture 2017-03-08 */
module.exports = function(grunt) {

  // Project configuration.
  grunt.initConfig({
    pkg: grunt.file.readJSON('package.json'),
    concat: {
      options: {
        banner: '/*! <%= pkg.name %> <%= grunt.template.today("yyyy-mm-dd") %> */\n'
      },
      build: {
        src: '*',
        dest: 'target/<%= pkg.name %>.min.js'
      }
    }
  });

  // Load the plugin that provides the "uglify" task.
  //grunt.loadNpmTasks('grunt-contrib-uglify');
  grunt.loadNpmTasks('grunt-contrib-concat');

  // Default task(s).
  grunt.registerTask('default', ['concat']);

};

var common= require('../common/util');
exports.handler = (event, context, callback) => {
    
    // perform any setup - here prepending correlationId in logs.
    var correlationId=common.initCorrelation();
    var response={};
    response["correlationId"]=correlationId;
    
    console.log('returning',response);
    callback(null, response);
};





{
  "name": "proceso-coordinate-capture",
  "version": "1.0.0",
  "description": "package to install capture and correlate function within coordinate",
  "main": "CaptureAndCorrelate.js",
  "dependencies": {
    "build-bundle": "^2.0.8",
    "common": "file:///Users/bonnerca/git/aminalz/proceso-coordinate/src/main/js/common",
    "grunt-cli": "^1.2.0",
    "uuid": "^3.0.1"
  },
  "devDependencies": {
    "aws-lambda-mock-context": "^3.0.1",
    "chai": "^3.5.0",
    "grunt": "^1.0.1",
    "grunt-contrib-concat": "^1.0.1",
    "mocha": "^3.2.0"
  },
  "scripts": {
    "bundle": "build-bundle . -o dist -v 1.0.0",
    "bundle-watch": "build-bundle . -o dist -v 1.0.0 -w",
    "test": "mocha test"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/cass-bonner/aminalz-ux.git"
  },
  "keywords": [
    "correlate",
    "lambda",
    "function"
  ],
  "author": "Cassandra Bonner",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/cass-bonner/aminalz-ux/issues"
  },
  "homepage": "https://github.com/cass-bonner/aminalz-ux#readme"
}


